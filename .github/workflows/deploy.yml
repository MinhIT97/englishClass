name: Auto Deploy

on:
  push:
    branches: [master]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Deploy Laravel
        run: |
          cd /home/minhnv/projects/englishClass
          git pull origin master
          docker compose up -d --build
